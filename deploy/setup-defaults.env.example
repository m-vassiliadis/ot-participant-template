# Default configuration values for setup-participant.sh
# Copy this file to setup-defaults.env and customize the values

# Participant name
PARTICIPANT_NAME=consumer

# Domain name for the participant
DOMAIN_NAME=

# Participant root folder (supports $USER_WORKING_DIR variable)
PARTICIPANT_ROOT_FOLDER=$USER_WORKING_DIR/participants

# Proxy folder path
PROXY_FOLDER=$USER_WORKING_DIR/reverse-proxy/caddy

# Use Let's Encrypt for SSL certificates
USE_LETSENCRYPT=true

# OPENTUNITY Identity Provider URL
OPENTUNITY_IDP_URL=https://idm.opentunity.que-tech.com

# Trust anchor DID
ISSUER_DID=did:web:idm.opentunity.que-tech.com:wallet-api:registry:6525bd9c-7010-49be-a9ff-358fb1649c5f

# API key to access the Issuer API (leave empty if not available)
ISSUER_API_KEY=

# OpenAPI URL for connector API (leave empty if connector is consumer-only)
CONNECTOR_OPENAPI_URL=
